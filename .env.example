############################
# Core / Django
############################
DJANGO_SECRET_KEY=change-me-in-production-use-strong-random-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0
DJANGO_TIME_ZONE=Asia/Almaty
DJANGO_LANGUAGE_CODE=ru
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
PYTHONUNBUFFERED=1

############################
# Database (PostgreSQL)
############################
POSTGRES_DB=zhilyego
POSTGRES_USER=zhilyego
POSTGRES_PASSWORD=zhilyego

# Django DB settings (должны совпадать с POSTGRES_*)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=zhilyego
DB_USER=zhilyego
DB_PASSWORD=zhilyego
DB_HOST=db
DB_PORT=5432

############################
# Redis / Celery
############################
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0

############################
# Encryption
############################
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=generate-new-key-with-command-above

############################
# Email (для разработки используется console backend)
############################
DEFAULT_FROM_EMAIL=noreply@zhilyego.local
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

############################
# Bots & External APIs (опционально)
############################
TELEGRAM_BOT_TOKEN=your-telegram-bot-token-here
BOT_SERVICE_USERNAME=bot_user
WEBHOOK_SECRET=your-webhook-secret

# WhatsApp Business (опционально)
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_VERIFY_TOKEN=

############################
# Payments - Kaspi (опционально)
############################
KASPI_API_KEY=
KASPI_MERCHANT_ID=
KASPI_SECRET_KEY=
KASPI_API_BASE_URL=https://api.kaspi.kz/v2/
AUTO_CONFIRM_PAYMENTS=false

############################
# Storage - S3/MinIO (опционально)
############################
S3_ENABLED=false
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET_NAME=zhilyego-media
S3_REGION=us-east-1
S3_ADDRESSING_STYLE=path
S3_USE_SSL=false
S3_ENDPOINT_URL=http://localhost:9000
S3_PUBLIC_BASE=http://localhost:9000/zhilyego-media

# AWS-совместимые алиасы
AWS_ACCESS_KEY_ID=${S3_ACCESS_KEY}
AWS_SECRET_ACCESS_KEY=${S3_SECRET_KEY}
AWS_STORAGE_BUCKET_NAME=${S3_BUCKET_NAME}
AWS_S3_REGION_NAME=${S3_REGION}
AWS_CLOUDFRONT_DOMAIN=

############################
# URLs / Domain
############################
SITE_URL=http://localhost:8000
DJANGO_DOMAIN=http://localhost:8000

############################
# Upload limits
############################
FILE_UPLOAD_MAX_MEMORY_SIZE=5242880
DATA_UPLOAD_MAX_MEMORY_SIZE=5242880
PHOTO_MAX_SIZE=5242880

############################
# Sentry (опционально)
############################
SENTRY_DSN=

############################
# ngrok (для тестирования Telegram bot webhook)
############################
NGROK_AUTHTOKEN=
